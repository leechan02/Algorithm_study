#include <iostream>
#include <algorithm>

int t, w, plum[1001], dp;

int dfs(int start) {
  if (start == w) {
    return 
  }
}

int main() {
  std::cin >> t >> w;
  for (int i = 0; i < t; ++i) {
    std::cin >> plum[i];
  }
  std::cout << dfs(0) << "\n";
}