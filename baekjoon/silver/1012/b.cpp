#include <iostream>
#include <vector>

typedef std::pair<int, int> position;
std::vector<int> map[50];
std::vector<int> visited[50];

int main() {
  int t;
  std::cin >> t;
  while (t--) {
    int m, n, k;
    std::cin >> m >> n >> k;

  }
}